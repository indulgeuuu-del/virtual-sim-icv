# 团队协作说明

本仓库用于四人共同开发2027届智能网联汽车设计赛项。统一采用“本地修改 → 验证 → 同步远端 → 直接推送main”，AI辅助开发也遵循相同流程。

## 文档入口

| 文件 | 内容 |
| --- | --- |
| [团队共享开发进度](开发路线.md) | 当前状态、已完成工作、验证结果、待办和阻塞 |
| [仿真环境准备](仿真环境准备.md) | 平台获取、环境安装、检查命令与CARLA运行步骤 |
| 本文 | 下载仓库、同步修改、处理冲突和共享资料 |

历史感知模块的运行说明保存在[perception/README.md](../../perception/README.md)。

## 文档编写约定

所有使用教程以队员从新电脑克隆仓库开始为前提：写清支持的系统、软件与依赖版本、资料获取方式、首次配置、日常运行、成功标准、输出位置及常见失败处理。仓库内使用相对路径，命令注明执行目录和终端类型；个人安装位置集中配置，不写死某位成员的盘符或用户名。

命令按顺序可执行，重复运行使用新的输出目录；不默认队员已有本地虚拟环境、隐藏文件、模型或许可证。未入库资料必须说明来源、放置方式及校验方法，尚未建立共享入口时明确标注缺口。实测结果注明适用环境，不把一台电脑通过写成全队或所有系统均已验证。

## 首次下载

安装Git，在PowerShell执行：

```powershell
git clone https://github.com/indulgeuuu-del/virtual-sim-icv.git
cd virtual-sim-icv
git status
```

下载后默认使用main。按环境准备文档安装当前任务需要的环境；仿真安装包、模型和视频不包含在仓库中。公开仓库允许读取，推送需要仓库所有者在GitHub的Settings → Collaborators中添加协作者，且对方接受邀请。实际权限以GitHub设置为准。

## 每次修改的流程

开始前用git status检查本地改动，保存好已有工作；工作树干净后同步：

```powershell
git switch main
git pull --ff-only origin main
# 编辑文件，运行对应检查或实验
git diff
git add 具体修改的文件
git diff --cached
git commit -m "说明解决了什么问题"
git pull --rebase origin main
# 若引入队友改动，重新验证受影响部分
git push origin main
```

commit保存本地版本，push发布到GitHub。修改前在团队内说明正在改的文件或接口，减少同时改动同一处造成的冲突；涉及公共接口时说明输入输出变化及影响范围。

pull --rebase会把自己尚未发布的提交接到队友最新提交之后。出现冲突时先停止，核对双方修改意图，保留需要的逻辑；解决后执行git add和git rebase --continue，重新验证再推送。不确定时用git rebase --abort返回同步前状态，与相关队友核对。

推送提示non-fast-forward时，再次同步、解决冲突并验证，不使用--force覆盖队友提交。已发布的错误通过新的修复提交纠正。

## 更新共享进度

有实质代码进展时更新[团队共享开发进度](开发路线.md)：只记录远端已有代码能力、对应验证和直接影响开发的待办或阻塞。本机安装、资料调研、文档整理不作为开发进度。提交说明记录对应修改，实验记录保存代码版本和参数，方便其他人复现。

代码检查、离线测试、CARLA运行、官方平台运行和官方评分分别记录。文档整理只需检查内容和链接；算法或接口修改运行对应测试，不能用已有测试通过代替本轮验证。

## 源码与资料共享

code_pre/保存学长交接原件，不直接修改。实质开发在独立副本中进行并记录来源，保留原作者与第三方版权信息。

源码、测试、必要配置和团队文档进入Git。AI记录、原始私人材料、模型、视频、安装包、缓存、实验输出和凭据留在本地，所需大文件通过团队约定的私有位置共享。提交前检查暂存清单，.gitignore不能代替人工核对。

实验记录包含代码版本、平台/SDK/依赖版本、地图、车型、场景、参数、结果与限制；涉及模型或视频时额外记录SHA256和人工真值。不要把账号密码或许可证写入仓库。
