# 团队共享开发进度

更新日期：2026-09-22。仅记录远端代码能力、验证范围和直接影响正式开发的待办。

## 当前状态

正式驾驶与泊车共同开发副本已建立于`competition/`，记录77份基线文件来源与哈希。策略循环已修复首帧负索引、空列表进入循环及小数停车时间截断；本届SDK完整构建、平台运行和官方评分尚未验证。

| 内容 | 仓库入口 | 完成程度 |
| --- | --- | --- |
| 驾驶与泊车开发副本 | `competition/` | 统一开发入口；原件保留，尚未完成本届适配 |
| 策略循环回归 | [test_competition_manual.py](../../tools/test_competition_manual.py) | 实际循环配合离线替身：7项通过；同套测试原版7项因非法索引失败。覆盖空列表、默认/显式参数、首帧建路、插值入口、切点、小数停车、重入；不代表真实SDK编译或车辆运动通过 |
| 同帧邻域重建 | [test_competition_neighborhood.py](../../tools/test_competition_neighborhood.py) | 将邻域计算移至本帧障碍物列表完成后；9项离线测试及调用顺序检查通过，原顺序9项反例失败。测试提取真实分类/查询函数，调度及SDK输入简化，未执行完整驾驶循环 |
| C++环境检查 | [check_driving_environment.py](../../tools/check_driving_environment.py)、[探测程序](../../tests/cpp/toolchain_smoke.cpp) | 支持编译运行小程序和盘点SDK文件；不等于完整比赛工程构建通过 |
| 辅助验证 | `tools/`、`tests/`、`perception/` | 已有CARLA控制实验及历史感知代码，尚未接入正式驾驶/泊车程序 |

## 接下来做什么

| 顺序 | 工作 | 验收目标 |
| --- | --- | --- |
| 当前 | 修复配置输入检查 | 策略解析拒绝缺列、错误类型及空内容，给出诊断并保留反例与回归 |
| 接着 | 修复基础控制问题 | 核对切点参数一致性、路径失败处理及连续比较；验证取数失败时行为及帧时间一致性 |
| 随后 | 提取并验证独立逻辑 | 明确配置、几何与决策输入输出，避免依赖错误的单位或帧数据 |
| SDK可用后 | 接入官方例程与实际场景 | 完成真实构建、连续控制及分组场景验证，记录规则对应的扣分 |
| 场景覆盖后 | 综合回归与定版 | 验证连续赛道及不同初始条件，固定可复现的代码、配置和提交步骤 |

仍待修复：C++连续比较错误、策略文件检查6列却读取第7列，以及解析器接受空策略。循环已对空列表报错，但解析器尚未给出完整诊断。停车计时仍依赖计算机时钟，未验证车辆实际静止时长。邻域顺序修复不代表平台消息时间戳同步或取数失败处理已验证。

## 当前缺口

尚未接入匹配本届的SDK、许可、地图和场景；旧场景编号、固定坐标及车型参数仍需逐项核对。固定配置根目录尚待改为可配置入口，完整工程构建仍依赖SDK上层工程。这不妨碍继续离线验证基础逻辑。
